<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.1.0 https://maven.apache.org/xsd/maven-4.1.0.xsd"
         >
    <modelVersion>4.1.0</modelVersion>

 <parent>
        <groupId>network.ike.platform</groupId>
        <artifactId>ike-parent</artifactId>
        <version>104</version>
        <relativePath/>
    </parent>

    <artifactId>komet-desktop</artifactId>
    <version>3.0.0-claude-assistant-SNAPSHOT</version>

    <name>Komet Desktop</name>

    <properties>
        <java.version>25</java.version>
        <!-- Configure build timestamp -->
        <maven.build.timestamp.format>yyyy-MM-dd HH:mm:ss</maven.build.timestamp.format>
        <!-- asm.version inherited from ike-parent:14 (9.9.1, see #283) -->
        <komet-bom.version>3.0.7-claude-assistant-SNAPSHOT</komet-bom.version>
        <!-- komet-claude-plugin is staged into the runtime plugins/ layer by the
             maven-dependency-plugin fetch-*-application-extensions copy executions
             (NOT a compile dependency — it loads via the plugin layer, exactly like
             rocks-kb-engine). -->
        <komet-claude-plugin.version>1-SNAPSHOT</komet-claude-plugin.version>
        <!-- Default: skip signing. Enable with -Dgpg.sign=true -->
        <gpg.sign.skip>true</gpg.sign.skip>

        <!-- JPackage output destination (used by create-desktop-installer profile) -->
        <jpackage.app.dest>target/dist/installer</jpackage.app.dest>
        <!-- JProfiler agent path (auto-detected by platform) -->
        <jprofiler.agent.path>/Applications/JProfiler.app/Contents/Resources/app/bin/macos/libjprofilerti.jnilib
        </jprofiler.agent.path>
        <!-- macOS codesigning identity for native libraries (used by ike:codesign-natives).
             Must match the signingKeyUsername in jreleaser.yml.
             Blank on non-macOS or unsigned builds — the goal skips gracefully. -->
        <codesign.identity>Developer ID Application: Keith Eugene Campbell (8WUE5BZ4BP)</codesign.identity>
        <skip.image.standard>false</skip.image.standard>
        <skip.image.profile>true</skip.image.profile>
        <skip.image.debug>true</skip.image.debug>
        <jreleaser.jlink.standard.active>ALWAYS</jreleaser.jlink.standard.active>
        <jreleaser.jlink.profile.active>NEVER</jreleaser.jlink.profile.active>
        <jreleaser.jlink.debug.active>NEVER</jreleaser.jlink.debug.active>

        <!--
            Build-stage skip properties. Settable directly via -Dname=true, or
            in bulk via the fast-dev / skip-codesign / skip-notarize profiles
            (see <profiles> below).

            codesign.{standard,debug,profile}.skip default to the per-image-variant
            skip so existing behavior is preserved; skip-codesign / fast-dev override
            them bulk-style to disable signing regardless of which images were built.
        -->
        <skip.installer>false</skip.installer>
        <codesign.standard.skip>${skip.image.standard}</codesign.standard.skip>
        <codesign.debug.skip>${skip.image.debug}</codesign.debug.skip>
        <codesign.profile.skip>${skip.image.profile}</codesign.profile.skip>
        <codesign.pkg.skip>false</codesign.pkg.skip>
        <notarize.skip>false</notarize.skip>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.ikm.komet</groupId>
                <artifactId>komet-bom</artifactId>
                <version>3.0.7-claude-assistant-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>dev.ikm.jpms</groupId>
            <artifactId>apfloat</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.hybrid-reasoner</groupId>
            <artifactId>hybrid-reasoner-snomed</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.elk</groupId>
            <artifactId>elk-snomed</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar.ext.owl</groupId>
            <artifactId>owl-extension</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.jpms</groupId>
            <artifactId>mvstore</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jgrapht</groupId>
            <artifactId>jgrapht-core</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.jpms</groupId>
            <artifactId>activej-bytebuf</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>tinkar-schema</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>collection</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>component</artifactId>
        </dependency>
        <dependency>
            <groupId>org.carlfx</groupId>
            <artifactId>cognitive</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pixelduke</groupId>
            <artifactId>fxcomponents</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>kview</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>knowledge-layout-editor</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>artifact</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>builder</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>classification</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>details</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>list</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>navigator</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>rules</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>search</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>progress</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>executor</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>executor-provider</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>entity</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>entity-provider</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>search-provider</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.lucene</groupId>
            <artifactId>lucene-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.lucene</groupId>
            <artifactId>lucene-queryparser</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.lucene</groupId>
            <artifactId>lucene-queries</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.lucene</groupId>
            <artifactId>lucene-highlighter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.lucene</groupId>
            <artifactId>lucene-suggest</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>data-mvstore-provider</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>data-spinedarray-provider</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>data-ephemeral-provider</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>changeset-writer-provider</artifactId>
        </dependency>

        <!-- Start JPro related dependencies -->
        <dependency>
            <groupId>one.jpro</groupId>
            <artifactId>jpro-webapi</artifactId>
        </dependency>
        <dependency>
            <groupId>one.jpro.platform</groupId>
            <artifactId>jpro-auth-core</artifactId>
        </dependency>
        <dependency>
            <groupId>one.jpro.platform</groupId>
            <artifactId>jpro-utils</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>jfx-incubator-richtext</artifactId>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark</artifactId>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark-ext-gfm-tables</artifactId>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark-ext-gfm-strikethrough</artifactId>
        </dependency>
        <dependency>
            <groupId>network.ike.platform</groupId>
            <artifactId>ike-commonmark-attributes</artifactId>
        </dependency>
        <!-- jpro-utils requires org.jetbrains.annotations at runtime/jlink -->
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>26.0.2</version>
        </dependency>
        <!--        testing -->
        <dependency>
            <groupId>one.jpro.platform.jpms</groupId>
            <artifactId>testfx-core</artifactId>
        </dependency>
        <dependency>
            <groupId>one.jpro.platform.jpms</groupId>
            <artifactId>testfx-junit5</artifactId>
        </dependency>
        <dependency>
            <groupId>one.jpro.platform.jpms</groupId>
            <artifactId>openjfx-monocle</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- End JPro related dependencies -->
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>composer</artifactId>
        </dependency>
        <dependency>
            <groupId>org.controlsfx</groupId>
            <artifactId>controlsfx</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
        </dependency>
        <dependency>
            <groupId>one.jpro.platform.jpms</groupId>
            <artifactId>cssfx</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.jpms</groupId>
            <artifactId>jsr305</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>sync</artifactId>
        </dependency>
        <!--
            rocks-kb-engine is NOT a compile dependency — it is loaded at runtime
            via the plugin layer (see IkeServiceManager and the fetch-*-application-extensions
            executions that stage it into plugins/). First real-world test of the plugin system.
        -->
        <!-- rocksdbjni-jpms moved to OS-specific profiles below -->
        <dependency>
            <groupId>dev.ikm.jpms</groupId>
            <artifactId>jna</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.jpms</groupId>
            <artifactId>jna-platform</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Explicitly manage granular JUnit 5 artifacts used by tests across modules -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>dev.ikm.jpms</groupId>
            <artifactId>jheaps</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>events</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>terms</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>preferences</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.komet</groupId>
            <artifactId>framework</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.ikm.tinkar</groupId>
            <artifactId>common</artifactId>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>3.2.8</version>
                    <configuration>
                        <signer>bc</signer>
                    </configuration>
                </plugin>
                <!-- Pin plugin versions locally so verify does not depend on parent pluginManagement -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.14.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.4.0</version>
                    <configuration>
                        <nonFilteredFileExtensions>
                            <nonFilteredFileExtension>icns</nonFilteredFileExtension>
                            <!-- Consider adding other binaries too -->
                            <nonFilteredFileExtension>ico</nonFilteredFileExtension>
                            <nonFilteredFileExtension>png</nonFilteredFileExtension>
                            <nonFilteredFileExtension>jpg</nonFilteredFileExtension>
                            <nonFilteredFileExtension>jpeg</nonFilteredFileExtension>
                            <nonFilteredFileExtension>gif</nonFilteredFileExtension>
                            <nonFilteredFileExtension>pdf</nonFilteredFileExtension>
                        </nonFilteredFileExtensions>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>3.9.0</version>
                    <!--
                       Configuration moved to Plugin Level.
                       This allows all 'copy' executions (fetch-*) to inherit this list.
                       The 'copy-dependencies' executions will safely ignore 'artifactItems'.
                    -->
                    <configuration>
                        <overWriteReleases>false</overWriteReleases>
                        <overWriteSnapshots>true</overWriteSnapshots>
                        <artifactItems>
                            <artifactItem>
                                <groupId>dev.ikm.ike</groupId>
                                <artifactId>rocks-kb-engine</artifactId>
                            </artifactItem>
                            <artifactItem>
                                <groupId>network.ike.komet</groupId>
                                <artifactId>komet-claude-plugin</artifactId>
                                <version>${komet-claude-plugin.version}</version>
                            </artifactItem>
                            <!-- Shared Markdown renderer (#585) the plugin requires at runtime.
                                 Staged into the plugins/ layer alongside the plugin (like
                                 rocksdbjni/jna are for rocks-kb-engine); it reads commonmark +
                                 the jfx richtext model from the boot image. Version BOM-managed
                                 (komet-bom). Built before this copy via the plugin's own edge. -->
                            <artifactItem>
                                <groupId>dev.ikm.komet</groupId>
                                <artifactId>komet-markdown-richtext</artifactId>
                            </artifactItem>
                            <artifactItem>
                                <groupId>dev.ikm.jpms</groupId>
                                <artifactId>rocksdbjni</artifactId>
                            </artifactItem>
                            <artifactItem>
                                <groupId>dev.ikm.jpms</groupId>
                                <artifactId>jna</artifactId>
                            </artifactItem>
                            <artifactItem>
                                <groupId>dev.ikm.jpms</groupId>
                                <artifactId>jna-platform</artifactId>
                            </artifactItem>
                        </artifactItems>
                    </configuration>
                    <!-- Reactor-ordering edge ONLY (not a compile/module-path dependency):
                         a plugin-level dependency on a reactor-built plugin makes Maven's
                         reactor sort build it before this module's copy execution stages it,
                         so a parallel `-T1C` build doesn't race the artifactItem copy. The
                         plugin stays out of module-jars/ (boot layer) — staging is still the
                         artifactItem copy into plugins/. Version shared with the artifactItem.
                         (rocks-kb-engine wants the same edge, but its version is BOM-managed
                         with no local property here — see ike-issues#583.) -->
                    <dependencies>
                        <dependency>
                            <groupId>network.ike.komet</groupId>
                            <artifactId>komet-claude-plugin</artifactId>
                            <version>${komet-claude-plugin.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>
                <plugin>
                    <groupId>org.jreleaser</groupId>
                    <artifactId>jreleaser-maven-plugin</artifactId>
                    <version>1.22.0</version>
                    <configuration>
                        <configFile>${project.build.directory}/jreleaser-config/jreleaser.yml</configFile>
                        <outputDirectory>${project.build.directory}/jreleaser</outputDirectory>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.3</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>3.1.4</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <!-- Filter jreleaser.yml so Maven properties are resolved before JReleaser reads it -->
                    <execution>
                        <id>filter-jreleaser-config</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/jreleaser-config</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/jreleaser</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>

                    <!-- log4j2.xml copy is now handled by JReleaser jlink fileSets -->

                    <!-- Copy JReleaser generated launcher scripts over jlink minimal launchers.
                         JReleaser generates proper launchers with JAVA_OPTS into inputs/bin/ but
                         jlink overwrites them with minimal scripts via its launcher flag. -->
                    <execution>
                        <id>fix-standard-launcher</id>
                        <phase>package[2]</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.standard}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-standard/jlink/${jreleaser.platform.work}/kometRuntimeImage-${jreleaser.platform.suffix}/bin</outputDirectory>
                            <overwrite>true</overwrite>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/jreleaser-jlink/assemble/komet-standard/jlink/inputs/bin</directory>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fix-debug-launcher</id>
                        <phase>package[2]</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.debug}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-debug/jlink/${jreleaser.platform.work}/kometDebugRuntimeImage-${jreleaser.platform.suffix}/bin</outputDirectory>
                            <overwrite>true</overwrite>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/jreleaser-jlink/assemble/komet-debug/jlink/inputs/bin</directory>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fix-profile-launcher</id>
                        <phase>package[2]</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.profile}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-profile/jlink/${jreleaser.platform.work}/kometProfileRuntimeImage-${jreleaser.platform.suffix}/bin</outputDirectory>
                            <overwrite>true</overwrite>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/jreleaser-jlink/assemble/komet-profile/jlink/inputs/bin</directory>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <!-- Copy installer artifacts (*.pkg, *.dmg, *.exe, *.msi, *.deb, *.rpm)
                         directly into target/ for easy access -->
                    <execution>
                        <id>copy-installers</id>
                        <!-- verify[3]: sequence AFTER notarize-installer
                             (verify[2]). At verify[2] the maven-resources
                             copy would race ahead of ike:notarize in plugin
                             declaration order, copying the pre-staple .pkg
                             to target/ and leaving the stapled version only
                             in target/jreleaser/.../jpackage/. See
                             IKE-Network/ike-issues#551. -->
                        <phase>verify[3]</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/jreleaser/assemble/komet/jpackage</directory>
                                    <includes>
                                        <include>*.pkg</include>
                                        <include>*.dmg</include>
                                        <include>*.exe</include>
                                        <include>*.msi</include>
                                        <include>*.deb</include>
                                        <include>*.rpm</include>
                                    </includes>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
                Wipe the jlink staging directories before copy-dependencies /
                copy populate them. Without this, stale version-qualified JARs
                from a prior build (e.g. a feature branch before feature-finish)
                accumulate alongside the current-version JARs, and jlink rejects
                the module path with a silent exit-1 because it sees multiple
                versions of the same module. `mvn clean` works around it, but
                that is a big hammer for an iterative build. This plugin-clean
                execution runs before the dependency staging within the same
                prepare-package phase (Maven executes plugins in declaration
                order, and this plugin is declared ahead of maven-dependency-plugin
                below).

                excludeDefaultDirectories=true prevents the clean goal from
                wiping all of target/ — we only want the two jlink-related
                subtrees gone.

                See IKE-Network/ike-issues#161.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <executions>
                    <execution>
                        <id>clean-jlink-staging</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                        <configuration>
                            <excludeDefaultDirectories>true</excludeDefaultDirectories>
                            <filesets>
                                <fileset>
                                    <directory>${project.build.directory}/jlink-module-path</directory>
                                </fileset>
                                <fileset>
                                    <directory>${project.build.directory}/jreleaser-jlink</directory>
                                </fileset>
                            </filesets>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <!-- Stage all dependency jars for JReleaser jlink module-path resolution -->
                    <execution>
                        <id>stage-dependencies-for-jlink</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/jlink-module-path</outputDirectory>
                            <includeScope>runtime</includeScope>
                            <excludeGroupIds>org.apfloat</excludeGroupIds>
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-standard-module-dependencies</id>
                        <phase>package[3]</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.standard}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-standard/jlink/${jreleaser.platform.work}/kometRuntimeImage-${jreleaser.platform.suffix}/module-jars</outputDirectory>
                            <!-- artifactItems are INHERITED from pluginManagement! -->
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-debug-module-dependencies</id>
                        <phase>package[3]</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.debug}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-debug/jlink/${jreleaser.platform.work}/kometDebugRuntimeImage-${jreleaser.platform.suffix}/module-jars
                            </outputDirectory>
                            <!-- artifactItems are INHERITED from pluginManagement! -->
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-profile-module-dependencies</id>
                        <phase>package[3]</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.profile}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-profile/jlink/${jreleaser.platform.work}/kometProfileRuntimeImage-${jreleaser.platform.suffix}/module-jars
                            </outputDirectory>
                            <!-- artifactItems are INHERITED from pluginManagement! -->
                        </configuration>
                    </execution>
                    <execution>
                        <id>fetch-standard-application-extensions</id>
                        <phase>package[4]</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.standard}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-standard/jlink/${jreleaser.platform.work}/kometRuntimeImage-${jreleaser.platform.suffix}/plugins</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fetch-debug-application-extensions</id>
                        <phase>package[4]</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.debug}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-debug/jlink/${jreleaser.platform.work}/kometDebugRuntimeImage-${jreleaser.platform.suffix}/plugins</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fetch-profile-application-extensions</id>
                        <phase>package[4]</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.profile}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-profile/jlink/${jreleaser.platform.work}/kometProfileRuntimeImage-${jreleaser.platform.suffix}/plugins
                            </outputDirectory>
                        </configuration>
                    </execution>
                    <!--
                        Stage the plugin-service-loader JAR into its own sibling directory.
                        IkeServiceManager.resolvePluginServiceLoaderPath() probes here (parent-of-bin
                        for jlink images). Must NOT land in module-jars/ — the service loader must
                        live in its own ModuleLayer, not the application boot modulepath.

                        Uses explicit <artifactItems> rather than inheriting from pluginManagement
                        (which lists native-lib deps for a different purpose).
                    -->
                    <execution>
                        <id>fetch-standard-plugin-service-loader</id>
                        <phase>package[4]</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.standard}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-standard/jlink/${jreleaser.platform.work}/kometRuntimeImage-${jreleaser.platform.suffix}/plugin-service-loader</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>dev.ikm.tinkar</groupId>
                                    <artifactId>plugin-service-loader</artifactId>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fetch-debug-plugin-service-loader</id>
                        <phase>package[4]</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.debug}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-debug/jlink/${jreleaser.platform.work}/kometDebugRuntimeImage-${jreleaser.platform.suffix}/plugin-service-loader</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>dev.ikm.tinkar</groupId>
                                    <artifactId>plugin-service-loader</artifactId>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fetch-profile-plugin-service-loader</id>
                        <phase>package[4]</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.image.profile}</skip>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink/assemble/komet-profile/jlink/${jreleaser.platform.work}/kometProfileRuntimeImage-${jreleaser.platform.suffix}/plugin-service-loader</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>dev.ikm.tinkar</groupId>
                                    <artifactId>plugin-service-loader</artifactId>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Make signing opt-in: only runs when -Dgpg.sign=true is passed -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                        <configuration>
                            <skip>${gpg.sign.skip}</skip>
                            <gpgArguments>
                                <argument>--pinentry-mode</argument>
                                <argument>loopback</argument>
                            </gpgArguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Compute build timestamp, platform, and JPackage version properties.

                 Also signs native libs (.jnilib, .dylib) inside JARs so jpackage
                 output passes Apple notarization. Runs after jlink + module-jar
                 copy + plugin fetch (package[4]) but before jpackage (verify).

                 Each codesign-*-natives execution has <skip>${codesign.X.skip}</skip>
                 which defaults to the per-image-variant skip (skip.image.X) — set
                 via the 'skip-codesign' or 'fast-dev' profiles (or -D flags) to
                 bulk-disable signing. -->
            <plugin>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-maven-plugin</artifactId>
                <!-- version from ike-parent pluginManagement -->
                <executions>
                    <execution>
                        <id>jpackage-props</id>
                        <goals>
                            <goal>jpackage-props</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>codesign-standard-natives</id>
                        <phase>package[5]</phase>
                        <goals>
                            <goal>codesign-natives</goal>
                        </goals>
                        <configuration>
                            <runtimeImageDir>${project.build.directory}/jreleaser-jlink/assemble/komet-standard/jlink/${jreleaser.platform.work}/kometRuntimeImage-${jreleaser.platform.suffix}</runtimeImageDir>
                            <signingIdentity>${codesign.identity}</signingIdentity>
                            <skip>${codesign.standard.skip}</skip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>codesign-debug-natives</id>
                        <phase>package[5]</phase>
                        <goals>
                            <goal>codesign-natives</goal>
                        </goals>
                        <configuration>
                            <runtimeImageDir>${project.build.directory}/jreleaser-jlink/assemble/komet-debug/jlink/${jreleaser.platform.work}/kometDebugRuntimeImage-${jreleaser.platform.suffix}</runtimeImageDir>
                            <signingIdentity>${codesign.identity}</signingIdentity>
                            <skip>${codesign.debug.skip}</skip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>codesign-profile-natives</id>
                        <phase>package[5]</phase>
                        <goals>
                            <goal>codesign-natives</goal>
                        </goals>
                        <configuration>
                            <runtimeImageDir>${project.build.directory}/jreleaser-jlink/assemble/komet-profile/jlink/${jreleaser.platform.work}/kometProfileRuntimeImage-${jreleaser.platform.suffix}</runtimeImageDir>
                            <signingIdentity>${codesign.identity}</signingIdentity>
                            <skip>${codesign.profile.skip}</skip>
                        </configuration>
                    </execution>
                    <!--
                        Sign the .pkg installer + notarize with Apple. Formerly in
                        the 'create-desktop-installer' profile. Each execution has
                        its own skip property so -Pskip-notarize can disable just
                        the ~10 min Apple round-trip while still signing the
                        installer, and -Pskip-codesign disables signing entirely.
                    -->
                    <execution>
                        <id>codesign-pkg-entitlements</id>
                        <phase>verify[1]</phase>
                        <goals>
                            <goal>codesign-pkg</goal>
                        </goals>
                        <configuration>
                            <entitlementsFile>${project.basedir}/src/main/resources/installer/resourceDir_unix/default.plist</entitlementsFile>
                            <signingIdentity>${codesign.identity}</signingIdentity>
                            <skip>${codesign.pkg.skip}</skip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>notarize-installer</id>
                        <phase>verify[2]</phase>
                        <goals>
                            <goal>notarize</goal>
                        </goals>
                        <configuration>
                            <skip>${notarize.skip}</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- REPLACED: build-helper-maven-plugin + maven-antrun-plugin by ike:jpackage-props above -->
            <!-- ASM plugin-realm injection moved to ike-parent:13 (#282) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <classpathDependencyExcludes>
                        <classpathDependencyExclude>dev.ikm.komet:executor</classpathDependencyExclude>
                    </classpathDependencyExcludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
            </plugin>
            <plugin>
                <groupId>org.jreleaser</groupId>
                <artifactId>jreleaser-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>jlink-images</id>
                        <phase>package[1]</phase>
                        <goals>
                            <goal>assemble</goal>
                        </goals>
                        <configuration>
                            <configFile>${project.build.directory}/jreleaser-config/jreleaser-jlink.yml</configFile>
                            <outputDirectory>${project.build.directory}/jreleaser-jlink</outputDirectory>
                        </configuration>
                    </execution>
                    <!--
                        Build the JPackage installer(s). Formerly inside the
                        'create-desktop-installer' profile with activeByDefault=true;
                        inlined here with <skip> on the execution so fast-dev /
                        skip-installer can opt out cleanly.
                    -->
                    <execution>
                        <id>Installer</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>assemble</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.installer}</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>scenic-view</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <dependencies>
                <!-- https://mvnrepository.com/artifact/net.raumzeitfalle.fx/scenic-view -->
                <dependency>
                    <groupId>net.raumzeitfalle.fx</groupId>
                    <artifactId>scenic-view</artifactId>
                </dependency>
                <!-- https://mvnrepository.com/artifact/org.openjfx/javafx-web -->
                <dependency>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx-web</artifactId>
                </dependency>
                <!-- https://mvnrepository.com/artifact/org.openjfx/javafx-swing -->
                <dependency>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx-swing</artifactId>
                </dependency>
            </dependencies>
        </profile>
        <!--
            The former 'create-desktop-installer' and 'codesign-natives' profiles
            have been inlined into the main <build> above. Two reasons:

              1. activeByDefault profiles get silently deactivated when ANY other
                 profile is explicitly activated — a common Maven gotcha that
                 would have fired for any user activating fast-dev/skip-notarize.

              2. Maven evaluates all profile activations in ONE pass against CLI
                 input only. Properties set by one profile (e.g. fast-dev setting
                 skip.installer=true) are NOT visible to another profile's
                 <activation><property> check — so !skip.installer activation
                 doesn't work for propagating fast-dev's intent.

            Instead, each execution now has <skip>${property}</skip> referencing
            a property whose default value preserves current behavior, and which
            the user-facing profiles below override to disable the step.
        -->

        <!--
            Build-mode profiles.

              fast-dev          AUTO-ACTIVE unless env var IKE_RELEASE is set.
                                Skips installer, codesign, notarize. Produces a
                                runnable jlink image at target/kometRuntimeImage/
                                without the Apple/Windows signing round-trip.
                                The default for IntelliJ, CLI dev builds, and
                                contributors without signing credentials.

              signed-installer  Opt-in: select with -P signed-installer, or
                                tick the box in IntelliJ's Maven tool window's
                                Profiles panel, or set env IKE_RELEASE=1
                                (release.env / TeamCity). Runs the full
                                jpackage + codesign + notarize + staple path.

              skip-codesign     Manual opt-in (composes with signed-installer).
                                Skip all code signing (natives + pkg) and, by
                                necessity, notarization. Installer still builds
                                but is unsigned. Useful for testing the
                                jpackage path without burning signing time.

              skip-notarize     Manual opt-in (composes with signed-installer).
                                Sign everything but skip the notarize step
                                (the wait on Apple's notary service). Useful
                                for local testing of signed output without the
                                ~10min Apple round-trip.

            Activation strategy:

              fast-dev uses <property><name>!env.IKE_RELEASE</name> instead
              of <activeByDefault>true</activeByDefault>. The latter is
              silently deactivated whenever ANY other profile (including the
              OS-family profiles `mac`, `linux`, `windows`) is active — see
              comment block above. fast-dev's env-property activation fires
              independently of any other active profile.

              signed-installer activates on env.IKE_RELEASE (so TeamCity
              hidden params and release.env both continue to work unchanged)
              OR on explicit `-P signed-installer` (the IntelliJ profile
              checkbox / CLI flag). The two activation methods are unioned.

              When the user does `-P signed-installer` without IKE_RELEASE
              set, fast-dev and signed-installer are BOTH active. Maven
              resolves the conflicting property values by profile declaration
              order — the later-declared profile wins. signed-installer is
              intentionally declared after fast-dev so it overrides the
              skips back to false and the release path runs. See
              IKE-Network/ike-issues#552 for the design rationale.

              skip-codesign and skip-notarize are declared LAST so they
              override both fast-dev and signed-installer when explicitly
              selected.
        -->
        <profile>
            <id>fast-dev</id>
            <activation>
                <property>
                    <name>!env.IKE_RELEASE</name>
                </property>
            </activation>
            <properties>
                <skip.installer>true</skip.installer>
                <codesign.standard.skip>true</codesign.standard.skip>
                <codesign.debug.skip>true</codesign.debug.skip>
                <codesign.profile.skip>true</codesign.profile.skip>
                <codesign.pkg.skip>true</codesign.pkg.skip>
                <notarize.skip>true</notarize.skip>
            </properties>
        </profile>
        <profile>
            <id>signed-installer</id>
            <!-- DECLARATION ORDER MATTERS: this profile must come AFTER
                 fast-dev so its property values win when both are active
                 (see the comment block above for why). Do not reorder. -->
            <activation>
                <property>
                    <name>env.IKE_RELEASE</name>
                </property>
            </activation>
            <properties>
                <skip.installer>false</skip.installer>
                <codesign.standard.skip>false</codesign.standard.skip>
                <!-- debug/profile image variants still default to skipped;
                     they're only built when separately requested. -->
                <codesign.debug.skip>true</codesign.debug.skip>
                <codesign.profile.skip>true</codesign.profile.skip>
                <codesign.pkg.skip>false</codesign.pkg.skip>
                <notarize.skip>false</notarize.skip>
            </properties>
        </profile>
        <profile>
            <id>skip-codesign</id>
            <properties>
                <codesign.standard.skip>true</codesign.standard.skip>
                <codesign.debug.skip>true</codesign.debug.skip>
                <codesign.profile.skip>true</codesign.profile.skip>
                <codesign.pkg.skip>true</codesign.pkg.skip>
                <notarize.skip>true</notarize.skip>
            </properties>
        </profile>
        <profile>
            <id>skip-notarize</id>
            <properties>
                <notarize.skip>true</notarize.skip>
            </properties>
        </profile>
        <!-- Platform properties (jreleaser.platform, .work, .suffix) now set by ike:jpackage-props -->
        <!-- Create symlinks from short paths to JReleaser jlink output -->
        <profile>
            <id>unix-jlink-symlinks</id>
            <activation>
                <os>
                    <family>unix</family>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>symlink-standard-image</id>
                                <phase>package[2]</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <skip>${skip.image.standard}</skip>
                                    <executable>ln</executable>
                                    <arguments>
                                        <argument>-sfn</argument>
                                        <argument>jreleaser-jlink/assemble/komet-standard/jlink/${jreleaser.platform.work}/kometRuntimeImage-${jreleaser.platform.suffix}</argument>
                                        <argument>${project.build.directory}/kometRuntimeImage</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>symlink-debug-image</id>
                                <phase>package[2]</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <skip>${skip.image.debug}</skip>
                                    <executable>ln</executable>
                                    <arguments>
                                        <argument>-sfn</argument>
                                        <argument>jreleaser-jlink/assemble/komet-debug/jlink/${jreleaser.platform.work}/kometDebugRuntimeImage-${jreleaser.platform.suffix}</argument>
                                        <argument>${project.build.directory}/kometDebugRuntimeImage</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>symlink-profile-image</id>
                                <phase>package[2]</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <skip>${skip.image.profile}</skip>
                                    <executable>ln</executable>
                                    <arguments>
                                        <argument>-sfn</argument>
                                        <argument>jreleaser-jlink/assemble/komet-profile/jlink/${jreleaser.platform.work}/kometProfileRuntimeImage-${jreleaser.platform.suffix}</argument>
                                        <argument>${project.build.directory}/kometProfileRuntimeImage</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>windows-jlink-symlinks</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>symlink-standard-image</id>
                                <phase>package[2]</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <skip>${skip.image.standard}</skip>
                                    <executable>cmd</executable>
                                    <arguments>
                                        <argument>/c</argument>
                                        <argument>mklink</argument>
                                        <argument>/D</argument>
                                        <argument>${project.build.directory}\kometRuntimeImage</argument>
                                        <argument>${project.build.directory}\jreleaser-jlink\assemble\komet-standard\jlink\${jreleaser.platform.work}\kometRuntimeImage-${jreleaser.platform.suffix}</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>symlink-debug-image</id>
                                <phase>package[2]</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <skip>${skip.image.debug}</skip>
                                    <executable>cmd</executable>
                                    <arguments>
                                        <argument>/c</argument>
                                        <argument>mklink</argument>
                                        <argument>/D</argument>
                                        <argument>${project.build.directory}\kometDebugRuntimeImage</argument>
                                        <argument>${project.build.directory}\jreleaser-jlink\assemble\komet-debug\jlink\${jreleaser.platform.work}\kometDebugRuntimeImage-${jreleaser.platform.suffix}</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>symlink-profile-image</id>
                                <phase>package[2]</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <skip>${skip.image.profile}</skip>
                                    <executable>cmd</executable>
                                    <arguments>
                                        <argument>/c</argument>
                                        <argument>mklink</argument>
                                        <argument>/D</argument>
                                        <argument>${project.build.directory}\kometProfileRuntimeImage</argument>
                                        <argument>${project.build.directory}\jreleaser-jlink\assemble\komet-profile\jlink\${jreleaser.platform.work}\kometProfileRuntimeImage-${jreleaser.platform.suffix}</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- Profiles to configure JPackage flags for Mac, Linux, & Windows Installers -->
        <profile>
            <id>mac</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>dev.ikm.jpms</groupId>
                    <artifactId>rocksdbjni-jpms</artifactId>
                    <version>10.4.2.r1-SNAPSHOT</version>
                    <classifier>osx</classifier>
                    <exclusions>
                        <exclusion>
                            <groupId>net.java.dev.jna</groupId>
                            <artifactId>jna</artifactId>
                        </exclusion>
                        <exclusion>
                            <groupId>net.java.dev.jna</groupId>
                            <artifactId>jna-platform</artifactId>
                        </exclusion>
                    </exclusions>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>linux</id>
            <activation>
                <os>
                    <family>unix</family>
                    <name>Linux</name>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>dev.ikm.jpms</groupId>
                    <artifactId>rocksdbjni-jpms</artifactId>
                    <version>10.4.2.r1-SNAPSHOT</version>
                    <classifier>linux64</classifier>
                    <exclusions>
                        <exclusion>
                            <groupId>net.java.dev.jna</groupId>
                            <artifactId>jna</artifactId>
                        </exclusion>
                        <exclusion>
                            <groupId>net.java.dev.jna</groupId>
                            <artifactId>jna-platform</artifactId>
                        </exclusion>
                    </exclusions>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>windows</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <properties>
                <!-- Platform properties and version computed by ike:jpackage-props -->
            </properties>
            <dependencies>
                <dependency>
                    <groupId>dev.ikm.jpms</groupId>
                    <artifactId>rocksdbjni-jpms</artifactId>
                    <version>10.4.2.r1-SNAPSHOT</version>
                    <classifier>win64</classifier>
                    <exclusions>
                        <exclusion>
                            <groupId>net.java.dev.jna</groupId>
                            <artifactId>jna</artifactId>
                        </exclusion>
                        <exclusion>
                            <groupId>net.java.dev.jna</groupId>
                            <artifactId>jna-platform</artifactId>
                        </exclusion>
                    </exclusions>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>jlink-profile</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <skip.image.profile>false</skip.image.profile>
                <jreleaser.jlink.profile.active>ALWAYS</jreleaser.jlink.profile.active>
            </properties>
        </profile>
        <profile>
            <id>jlink-debug</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <skip.image.debug>false</skip.image.debug>
                <jreleaser.jlink.debug.active>ALWAYS</jreleaser.jlink.debug.active>
            </properties>
        </profile>
    </profiles>
</project>
